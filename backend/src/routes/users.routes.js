'use strict';
const express=require('express'); const bcrypt=require('bcrypt'); const {body}=require('express-validator'); const db=require('../db'); const config=require('../config/env'); const asyncHandler=require('../utils/asyncHandler'); const {requireAuth}=require('../middleware/auth'); const {handleValidation}=require('../middleware/validate'); const {logAudit}=require('../utils/audit'); const {UnauthorizedError,NotFoundError}=require('../utils/errors');
const router=express.Router(); router.use(requireAuth);
router.get('/profile',asyncHandler(async(req,res)=>{const user=db.prepare('SELECT id,email,full_name,role,created_at FROM users WHERE id = ?').get(req.user.id);if(!user)throw new NotFoundError('User not found');const profile=db.prepare('SELECT date_of_birth,phone,bio FROM profiles WHERE user_id = ?').get(req.user.id)||{};res.json({profile:{id:user.id,email:user.email,fullName:user.full_name,role:user.role,createdAt:user.created_at,dateOfBirth:profile.date_of_birth||null,phone:profile.phone||null,bio:profile.bio||null}});}));
router.put('/profile',[body('fullName').optional().trim().isLength({min:1,max:200}),body('dateOfBirth').optional({nullable:true}).isISO8601(),body('phone').optional({nullable:true}).isString().isLength({max:40}),body('bio').optional({nullable:true}).isString().isLength({max:2000})],handleValidation,asyncHandler(async(req,res)=>{const{fullName,dateOfBirth,phone,bio}=req.body;if(fullName!==undefined)db.prepare("UPDATE users SET full_name=?,updated_at=strftime('%Y-%m-%dT%H:%M:%fZ','now') WHERE id=?").run(fullName,req.user.id);if(!db.prepare('SELECT user_id FROM profiles WHERE user_id=?').get(req.user.id))db.prepare('INSERT INTO profiles(user_id) VALUES(?)').run(req.user.id);db.prepare('UPDATE profiles SET date_of_birth=COALESCE(?,date_of_birth),phone=COALESCE(?,phone),bio=COALESCE(?,bio) WHERE user_id=?').run(dateOfBirth??null,phone??null,bio??null,req.user.id);logAudit({actorUserId:req.user.id,action:'profile.updated',ip:req.ip});res.json({message:'Profile updated'});}));
router.put('/password',[body('currentPassword').notEmpty(),body('newPassword').isLength({min:10}).matches(/[0-9]/).matches(/[A-Za-z]/)],handleValidation,asyncHandler(async(req,res)=>{const user=db.prepare('SELECT * FROM users WHERE id=?').get(req.user.id);if(!user)throw new NotFoundError('User not found');if(!await bcrypt.compare(req.body.currentPassword,user.password_hash))throw new UnauthorizedError('Current password is incorrect');const newHash=await bcrypt.hash(req.body.newPassword,config.bcryptCost);const now=new Date().toISOString();const tx=db.transaction(()=>{db.prepare("UPDATE users SET password_hash=?,password_changed_at=?,updated_at=strftime('%Y-%m-%dT%H:%M:%fZ','now') WHERE id=?").run(newHash,now,req.user.id);db.prepare("UPDATE refresh_tokens SET revoked_at=strftime('%Y-%m-%dT%H:%M:%fZ','now') WHERE user_id=? AND revoked_at IS NULL").run(req.user.id);});tx();logAudit({actorUserId:req.user.id,action:'auth.password_changed',ip:req.ip});res.json({message:'Password updated. Please log in again on other devices.'});}));
router.delete('/me',[body('password').notEmpty()],handleValidation,asyncHandler(async(req,res)=>{const user=db.prepare('SELECT * FROM users WHERE id=?').get(req.user.id);if(!user)throw new NotFoundError('User not found');if(!await bcrypt.compare(req.body.password,user.password_hash))throw new UnauthorizedError('Password is incorrect');logAudit({actorUserId:req.user.id,action:'account.deleted',ip:req.ip});db.prepare('DELETE FROM users WHERE id=?').run(req.user.id);res.status(204).end();}));
module.exports=router;
